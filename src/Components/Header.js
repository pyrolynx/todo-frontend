import Navbar from 'react-bootstrap/Navbar';

const Header = () => {
    return (
        <Navbar bg="light">
            <Navbar.Brand>This is PyroLynx ToDo list</Navbar.Brand>
        </Navbar>
    )
};

export default Header;